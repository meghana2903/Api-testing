public class FrontendAutoDatoms2 {
	private Webdriver driver;

	@BeforeMethod
	public void before() {
		driver = new ChromeDriver();
	}
	@Test(priority = 1)
	public void DatomsLogin()
	{
		driver.get(storingvalues.url);
		driver.findElement(By.xpath(storingvalues.emailxpath))
		.sendKeys(storingvalues.emailkeys);
		driver.findElement(By.cssSelector(storingvalues.passxpath))
		.sendKeys(storingvalues.passleys);
		driver.findElement(By.cssSelector(storingvalues.loginxpath))
		.click();


		@AfterMethod
		public void Exit() {
			driver.close();
		}
	}
}
